# 盆栽陪伴机器人开发文档

> 版本：V1.0  
> 项目定位：桌面 AI 陪伴 + 植物养护 + 情感交互  
> 主控平台：ESP32-P4  
> 图形框架：LVGL 8.4.0  
> 显示方案：ST7789（240×320）  
> 网络方案：ESP32-C6 / Wi-Fi  
> 语音方案：小智语音接入  
> 摄像头方案：USB Host + UVC  
> 系统框架：ESP-IDF + FreeRTOS

---

## 1. 项目目标

本项目开发一款“盆栽陪伴机器人”，将真实植物养护、AI 语音交互、情绪表情、环境感知与机器人动作融合。

第一阶段目标不是一次性完成所有产品功能，而是先搭建稳定的软件和硬件架构，再逐步接入：

- 屏幕显示与动态表情
- AI 语音交互
- 植物环境传感器
- 左右电容触摸
- 氛围呼吸灯
- USB 摄像头
- 旋转底座
- Wi-Fi / BLE 配网
- 云端通信
- 专注模式
- 电源管理
- OTA
- 故障处理与整机稳定性

---

## 2. 总体系统架构

```text
                         用户
                          │
             ┌────────────┼────────────┐
             │            │            │
            语音         触摸         App
             │            │            │
             ↓            ↓            ↓
         麦克风       电容触摸      BLE / WiFi
             │            │            │
             └──────┬─────┴─────┬──────┘
                    │           │
                    ↓           ↓
                ESP32-P4   ESP32-C6
                    │           │
                    │        Wi-Fi/BLE
                    │           │
          ┌─────────┼───────────┐
          │         │           │
          ↓         ↓           ↓
        LVGL       Audio      Network
          │         │           │
       ST7789     小智语音     云端服务
          │         │           │
          ├─────────┼───────────┤
          │         │           │
          ↓         ↓           ↓
       表情/UI    扬声器       AI/MCP
          │                     │
          └──────────┬──────────┘
                     ↓
                App Event Bus
                     │
        ┌────────────┼──────────────┐
        ↓            ↓              ↓
     Sensor       Motor          Camera
        │            │              │
 土壤/光照/温湿度   底座旋转        USB UVC
```

---

## 3. 软件分层架构

### 3.1 驱动层

负责直接操作硬件：

```text
display_driver
touch_driver
audio_driver
camera_driver
sensor_driver
motor_driver
led_driver
power_driver
wifi_driver
storage_driver
```

驱动层不处理业务状态。

### 3.2 中间层

```text
LVGL Port
Audio Codec
Opus Codec
UVC Manager
Network Manager
File System
Resource Manager
Time Manager
BLE Provisioning
```

### 3.3 服务层

```text
UI Service
Audio Service
Camera Service
Plant Service
Motor Service
Power Service
Xiaozhi Service
```

### 3.4 业务层

```text
UI State Manager
Expression Manager
AI Interaction Manager
Plant Care Manager
Focus Manager
Reminder Manager
Fault Manager
```

### 3.5 应用层

```text
App Event Bus
        ↓
App State Machine
        ↓
各业务模块
```

---

## 4. FreeRTOS 任务规划

| Task | 主要职责 |
|---|---|
| UI Task | LVGL、页面刷新、GIF/动画 |
| Audio Task | 麦克风采集、扬声器播放 |
| Xiaozhi Task | 小智协议、ASR/TTS/MCP |
| Network Task | Wi-Fi、HTTP、WebSocket |
| Touch Task | 左右触摸和手势识别 |
| Sensor Task | 土壤湿度、光照、温湿度 |
| Camera Task | USB Host、UVC、抓拍/视频 |
| Motor Task | 电机、编码器、回零 |
| Power Task | 电量、充电、休眠 |
| System Task | 时间、状态、健康监控 |

原则：

> 只有 UI Task 可以直接调用 LVGL，其它 Task 通过 Queue 给 UI Task 发送事件。

---

## 5. App Event Bus

```c
typedef enum
{
    APP_EVT_BOOT,

    APP_EVT_WIFI_CONNECTED,
    APP_EVT_WIFI_DISCONNECTED,

    APP_EVT_WAKEUP,
    APP_EVT_LISTEN_START,
    APP_EVT_LISTEN_STOP,

    APP_EVT_AI_PROCESSING,
    APP_EVT_AI_REPLY_START,
    APP_EVT_AI_REPLY_END,

    APP_EVT_EXPRESSION,

    APP_EVT_TOUCH_LEFT,
    APP_EVT_TOUCH_RIGHT,
    APP_EVT_TOUCH_RIGHT_DOUBLE,

    APP_EVT_LOW_BATTERY,
    APP_EVT_CRITICAL_BATTERY,

    APP_EVT_SENSOR_UPDATE,

    APP_EVT_CAMERA_SNAPSHOT,
    APP_EVT_CAMERA_STREAM_START,
    APP_EVT_CAMERA_STREAM_STOP,

    APP_EVT_MOTOR_HOME,
    APP_EVT_MOTOR_MOVE,

    APP_EVT_FOCUS_START,
    APP_EVT_FOCUS_PAUSE,
    APP_EVT_FOCUS_RESUME,
    APP_EVT_FOCUS_END,

    APP_EVT_FAULT,
} app_event_type_t;
```

所有模块尽量通过事件交互，而不是模块之间直接调用。

---

## 6. 屏幕显示模块

### 6.1 显示链路

```text
ESP32-P4
   ↓
SPI
   ↓
ST7789
   ↓
LVGL 8.4.0
   ↓
UI Page / GIF / Animation
```

### 6.2 页面规划

```text
PAGE_HOME
PAGE_EXPRESSION
PAGE_LISTENING
PAGE_THINKING
PAGE_REPLY
PAGE_SLEEP
PAGE_WARNING
PAGE_FOCUS_START
PAGE_FOCUS_RUNNING
PAGE_FOCUS_PAUSE
PAGE_FOCUS_END
```

50 个表情不要设计成 50 个独立页面。

### 6.3 表情系统

统一调用：

```c
expression_play(EXP_HAPPY);
expression_stop();
```

推荐类型：

```c
typedef enum
{
    EXP_TYPE_LVGL,
    EXP_TYPE_GIF,
    EXP_TYPE_FRAME
} expression_type_t;
```

资源建议：

- 简单表情：LVGL 原生动画
- 复杂特效：GIF
- 植物成长：图片帧序列

### 6.4 GIF

LVGL 8.4.0：

```c
#define LV_USE_GIF 1
```

MVP 阶段先做：

```text
neutral.gif
happy.gif
listen.gif
think.gif
sleep.gif
```

---

## 7. UI 状态机

```c
typedef enum
{
    UI_STATE_HOME,
    UI_STATE_LISTENING,
    UI_STATE_THINKING,
    UI_STATE_REPLY,
    UI_STATE_EXPRESSION,
    UI_STATE_SLEEP,
    UI_STATE_FOCUS_RUNNING,
    UI_STATE_FOCUS_PAUSE,
    UI_STATE_LOW_BATTERY,
    UI_STATE_FAULT
} ui_state_t;
```

正常流程：

```text
HOME
 ↓
LISTENING
 ↓
THINKING
 ↓
REPLY
 ↓
根据当前环境恢复
 ├─ HOME
 ├─ SLEEP
 ├─ FOCUS_RUNNING
 └─ FOCUS_PAUSE
```

---

## 8. UI 优先级

```text
P1 安全 / 故障 / OTA / 重启
 ↓
P2 极低电量 / 关机提示
 ↓
P3 AI 回复 / 重要主动提醒
 ↓
P4 AI 聆听 / AI 处理中
 ↓
P5 专注模式
 ↓
P6 左右普通翻页
 ↓
P7 时间 / 天气 / 定时表情 / 睡眠
```

所有页面切换经过：

```c
ui_state_request(new_state);
```

---

## 9. 时间首页

显示：

```text
时间
日期
星期
天气
网络状态
电量
```

刷新：

```text
时间：分钟变化刷新
天气：约每 30 分钟刷新
电量：低频刷新
网络：状态变化刷新
```

断网：

```text
时间继续使用本地 RTC
天气保留最近结果
无历史数据时显示 --℃
```

---

## 10. 专注模式

```text
FOCUS_START
      ↓
FOCUS_RUNNING
      ↕
FOCUS_PAUSE
      ↓
FOCUS_END
```

显示：

```text
浅米色背景
植物成长动画
本次专注时间
累计分钟
```

成长阶段：

```text
0~15min   双叶嫩芽
15~30min  嫩芽长高
30~60min  新叶展开
>=60min   成熟状态
```

---

## 11. 左右电容触摸

普通状态：

```text
左单击 → 上一页
右单击 → 下一页
右双击 → 进入专注
```

专注状态：

```text
右单击 → 暂停 / 继续
右双击 → 退出专注
左单击 → 无动作
```

建议：

```text
Touch Driver
     ↓
Debounce
     ↓
Gesture Recognizer
     ↓
App Event
```

---

## 12. 植物传感器

第一版：

```text
土壤湿度
光照
温度
空气湿度
```

统一数据结构：

```c
typedef struct
{
    float soil;
    float light;
    float temperature;
    float humidity;
} plant_status_t;
```

接口：

```c
plant_get_status(&status);
```

第一版只做检测 + 提醒，不做自动浇水。

---

## 13. 呼吸灯

功能：

```text
亮度控制
RGB 控制
呼吸动画
情绪联动
状态联动
```

示例：

```text
待机 → 柔和呼吸
AI 聆听 → 蓝色呼吸
AI 回复 → 情绪色
低电量 → 红色提示
故障 → 红色闪烁
```

---

## 14. 音频系统

输入：

```text
麦克风
 ↓
I2S
 ↓
PCM
 ↓
NS / VAD
 ↓
Opus Encoder
 ↓
小智服务器
```

输出：

```text
小智服务器
 ↓
Opus
 ↓
Opus Decoder
 ↓
PCM
 ↓
I2S
 ↓
功放
 ↓
扬声器
```

第一阶段先验证本地采集和播放，不先接 AI。

---

## 15. 小智语音接入

核心链路：

```text
用户
 ↓
MIC
 ↓
PCM
 ↓
Opus
 ↓
WebSocket
 ↓
小智 Server
 ↓
ASR
 ↓
LLM
 ↓
TTS / MCP
 ↓
ESP32-P4
```

### 15.1 ASR

```text
listen:start
 ↓
Opus Frames
 ↓
listen:stop
```

服务端：

```json
{
  "type": "stt",
  "text": "用户说的话"
}
```

### 15.2 小智与 UI 映射

```text
Wake Word → LISTENING
Listen End → THINKING
TTS Start → REPLY
Emotion → Expression Manager
TTS Stop → 恢复当前状态
```

---

## 16. MCP 设备能力

建议：

```text
plant.get_status
robot.rotate
robot.home
light.set_color
light.set_brightness
camera.snapshot
system.get_battery
```

示例：

```text
用户：“我的植物需要浇水吗？”
 ↓
LLM
 ↓
plant.get_status
 ↓
ESP32-P4 读取传感器
 ↓
返回真实数据
 ↓
AI 生成回复
 ↓
TTS
```

---

## 17. Wi-Fi / BLE 配网

```text
手机 App
 ↓
BLE Scan
 ↓
发现设备
 ↓
BLE Connect
 ↓
SSID + Password
 ↓
ESP32-C6
 ↓
连接 WiFi
 ↓
返回结果
 ↓
NVS 保存
```

开机：

```text
读取 NVS
 ↓
自动连接
 ↓
失败
 ↓
重新进入配网
```

---

## 18. USB 摄像头

基础链路：

```text
USB Camera
 ↓
USB Host
 ↓
UVC
 ↓
MJPEG / YUY2
 ↓
Frame Buffer
```

第一阶段：

```text
USB Camera
 ↓
UVC
 ↓
获取一张 JPEG
 ↓
HTTP 上传
```

视频链路：

```text
USB Camera
 ↓
UVC
 ↓
Frame Queue
 ↓
Video Task
 ↓
H.264 / MJPEG
 ↓
Network Queue
 ↓
HTTP / WebSocket
 ↓
Server
```

Camera API：

```c
camera_init();
camera_start();
camera_stop();
camera_snapshot();
camera_stream_start();
camera_stream_stop();
camera_set_resolution();
camera_set_fps();
```

---

## 19. 旋转底座

推荐结构：

```text
电机
 ↓
减速机构
 ↓
编码器
 ↓
转盘

霍尔 + 磁铁
 ↓
0° 原点
```

开发顺序：

```text
电机转动
 ↓
回零
 ↓
编码器反馈
 ↓
指定角度
 ↓
平滑加减速
 ↓
堵转检测
 ↓
AI 控制
```

API：

```c
motor_init();
motor_home();
motor_rotate_relative(float angle);
motor_rotate_to(float angle);
motor_stop();
```

第一版建议限制为：

```text
-170° ~ +170°
```

---

## 20. 电源管理

需要实现：

```text
电量检测
充电状态
低电量
极低电量
低功耗
自动休眠
异常断电保护
```

---

## 21. 异常处理

必须覆盖：

```text
WiFi 断开
AI Server 超时
BLE 配网中断
摄像头拔出
GIF 资源损坏
传感器异常
电机堵转
麦克风异常
扬声器异常
低电量
NVS 数据损坏
SD 卡异常
```

统一经过：

```text
Fault Manager
      ↓
App Event
      ↓
UI / Audio / Recovery
```

---

## 22. 工程目录建议

```text
project/
│
├── main/
│   ├── app_main.c
│   ├── app_event.c
│   ├── app_event.h
│   └── app_state.c
│
├── components/
│   ├── display/
│   ├── ui/
│   │   ├── ui_manager.c
│   │   ├── ui_state.c
│   │   ├── ui_expression.c
│   │   ├── pages/
│   │   └── assets/
│   ├── audio/
│   ├── xiaozhi/
│   ├── touch/
│   ├── sensor/
│   ├── camera/
│   ├── motor/
│   ├── led/
│   ├── network/
│   ├── provisioning/
│   ├── storage/
│   └── power/
│
└── CMakeLists.txt
```

---

# 23. 开发步骤

## 阶段 0：需求冻结

输出：

```text
硬件 BOM
GPIO 表
接口定义
系统框图
```

## 阶段 1：ESP32-P4 基础工程

```text
ESP-IDF
串口日志
FreeRTOS
PSRAM
Flash
NVS
```

## 阶段 2：屏幕

```text
ST7789
RGB 测试
LVGL 8.4
文字
图片
GIF
```

页面：

```text
HOME
LISTEN
THINK
REPLY
SLEEP
FAULT
```

## 阶段 3：UI 状态机

```text
HOME → LISTEN → THINK → REPLY → 恢复
```

加入优先级、覆盖、恢复、超时和异常。

## 阶段 4：传感器 + 触摸 + LED

完成：

```text
土壤湿度
光照
温湿度
左右触摸
呼吸灯
```

## 阶段 5：音频

```text
麦克风采集
扬声器播放
I2S
PCM
Opus
```

## 阶段 6：Wi-Fi

```text
C6 WiFi
自动连接
NVS
断线重连
HTTP
WebSocket
BLE 配网
```

## 阶段 7：小智

第一步：

```text
MIC → 小智 → AI 回复 → Speaker
```

第二步：

```text
小智状态 → UI 状态机
```

第三步：

```text
MCP → Sensor / Motor / LED / Camera
```

## 阶段 8：摄像头

```text
USB Host
 ↓
UVC
 ↓
获取 JPEG
 ↓
HTTP 上传
```

再做实时流。

## 阶段 9：旋转底座

```text
电机
编码器
原点
角度控制
平滑启停
堵转保护
```

## 阶段 10：专注模式

```text
进入
运行
暂停
继续
结束
植物成长
状态恢复
```

## 阶段 11：电源管理

```text
电量
充电
低电量
极低电量
休眠
背光
WiFi
Camera
Audio
```

## 阶段 12：整机联调

```text
AI + UI
AI + Sensor
AI + Motor
AI + Camera
UI + Touch
Focus + AI
Focus + Low Battery
Camera + Network
Audio + Network
Motor + Audio
```

## 阶段 13：稳定性

```text
72 小时连续运行
网络断线
服务器异常
USB 热插拔
电机连续动作
内存泄漏
温升
功耗
```

---

# 24. MVP 规划

## MVP1

```text
ST7789
LVGL
时间首页
5 个表情
植物传感器
呼吸灯
```

## MVP2

```text
麦克风
扬声器
WiFi
小智
AI 表情联动
```

闭环：

```text
用户说话
 ↓
AI
 ↓
语音回复
 ↓
表情同步
```

## MVP3

```text
BLE 配网
专注模式
USB Camera
图片上传
旋转底座
MCP
```

---

# 25. 最终验收重点

## 屏幕

- 页面切换正常
- 表情动画流畅
- 无明显闪屏
- 高优先级页面正确覆盖
- 状态结束能够恢复正确页面

## 音频

- 采集清晰
- TTS 无明显爆音
- AI 对话链路稳定

## 网络

- Wi-Fi 稳定
- 自动重连
- WebSocket 稳定
- HTTP 上传正常

## 摄像头

- UVC 采集稳定
- 抓拍成功
- 上传正常
- 拔插不会导致系统崩溃

## 电机

- 可回零
- 可指定角度
- 平滑启动/停止
- 异常阻力停止

## 系统

- 无明显内存泄漏
- 长时间运行无死机
- 异常能够恢复
- 模块之间不存在明显阻塞

---

# 26. 当前第一阶段执行清单

```text
[ ] 建立 ESP-IDF 工程
[ ] 确认 GPIO 分配
[ ] ST7789 点亮
[ ] LVGL 8.4.0 跑通
[ ] GIF 播放
[ ] HOME 页面
[ ] LISTEN 页面
[ ] THINK 页面
[ ] REPLY 页面
[ ] UI 状态机
[ ] 麦克风采集
[ ] 扬声器播放
[ ] WiFi 联网
[ ] 小智基础语音链路
```

完成后再进入：

```text
传感器
触摸
专注模式
摄像头
电机
MCP
低功耗
整机联调
```

---

# 27. 核心开发原则

1. 先单模块，再联调。
2. 所有业务通过事件驱动。
3. 其它任务不直接调用 LVGL。
4. AI 不直接操作具体硬件驱动。
5. 硬件能力统一封装成 Service / MCP。
6. 页面状态和业务状态分离。
7. 异常处理从开发初期预留。
8. 不要第一版就做全部 50 个表情。
9. 先完成最小闭环，再扩展功能。
10. 稳定性优先于功能数量。

---

# 28. 核心最小闭环

```text
开机
 ↓
ST7789 显示时间
 ↓
用户唤醒
 ↓
聆听表情
 ↓
麦克风采集
 ↓
小智 ASR / LLM
 ↓
思考表情
 ↓
TTS 返回
 ↓
扬声器播放
 ↓
对应情绪表情
 ↓
回复结束
 ↓
恢复时间首页
```

这个闭环跑通后，整个项目的核心架构基本成立。
