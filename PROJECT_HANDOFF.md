# Lummiss 盆栽陪伴机器人项目接手说明

更新时间：2026-09-08

## 1. 项目目标

当前负责范围是 **Lummiss 盆栽陪伴机器人嵌入式程序**，主控为 ESP32-P4。产品计划包含屏幕表情与状态交互、语音对话、植物状态采集、USB 摄像头、联网、执行机构、电源管理等能力。

当前阶段先逐项验证硬件可行性，再把通过验证的驱动拆成模块并集成产品状态机。不要直接在完整产品程序中同时调试所有外设。

产品需求参考文件位于：

- `项目文档/盆栽陪伴机器人_屏幕交互版.docx`
- `项目文档/嵌入式开发外包需求说明书_家用版.docx`
- `项目文档/嵌入式开发外包需求说明书_手办版.docx`

盆栽机器人应优先以第一份文档为交互需求基线，另外两份文档只用于了解公共的嵌入式能力。三份文档中的产品形态和职责范围并不完全相同，不能直接合并为一套验收要求。

## 2. 目录说明

项目根目录已使用纯英文路径：

```text
E:\Lummiss_Plant_Robot
```

主要目录：

```text
src/esp_draw_bit   开发板原始示例，保留用于参考，不在其中开发产品功能
src/demo           当前重新编写的 ESP32-P4 硬件可行性测试工程
项目文档           产品需求文档
开发板示例         厂商完整资料和其他示例
```

后续开发默认从 `src/demo` 继续。不要覆盖或清理 `src/esp_draw_bit`，其中的 BSP、音频、LVGL 和板级初始化仍有参考价值。

## 3. 当前工程结构

`src/demo` 是 ESP-IDF 5.5.5 工程，目标芯片为 ESP32-P4，当前支持两种独立测试模式：

| 模式 | 源文件 | 用途 | 状态 |
| --- | --- | --- | --- |
| `screen` | `main/screen_test.c` | GMT020-02-8P / ST7789 的 LVGL 三表情演示 | 编译通过，等待实机动画确认 |
| `camera` | `main/camera_test.c` | LRCPG720p 高速 USB UVC 摄像头测试 | 编译通过，旧会话中已实机稳定收帧 |

VS Code 当前默认配置为 `screen`，构建目录是 `build_screen_verified`，串口配置为 `COM17`。

当前主要组件版本：

| 组件 | 版本 | 用途 |
| --- | --- | --- |
| ESP-IDF | 5.5.5 | ESP32-P4 开发框架 |
| LVGL | 8.4.0 | 表情图形和动画 |
| `espressif/esp_lvgl_port` | 2.9.0 | LVGL 任务、时钟、SPI LCD 刷新与 DMA 缓冲 |
| `espressif/usb_host_uvc` | 2.2.0 | USB UVC 摄像头 |

## 4. 已完成工作

### 4.1 工程恢复与构建问题修复

- 从新导入的示例重新建立 `src/demo`，没有修改厂商示例。
- 根目录改为英文路径，规避 Python、Kconfig、CMake 和 Ninja 在中文路径下出现的 GBK/UTF-8 解码及乱码路径问题。
- 配置 ESP-IDF 5.5.5、ESP32-P4、16 MB Flash、PSRAM 和自定义分区表。
- 根据开发板串口识别结果，将目标芯片分支设为 ESP32-P4 v1.x（最低 v1.0、最高 v1.99）；该开发板实测为 v1.3，不能使用 IDF 默认的 v3.1 镜像。
- 将 Bootloader 日志级别调为 WARN，使 Bootloader 能够放入当前 `0x6000` 字节分区；v1.x 当前产物大小为 `0x5310`。
- 修正 CMake 多阶段依赖扫描问题。屏幕和摄像头模式统一声明必要组件，避免首次扫描使用默认模式时漏掉 `esp_timer` 等依赖。
- 修正 VS Code 构建参数：Windows 使用 `idf.buildPathWin`，并保留 ESP-IDF 扩展默认的 `-G Ninja`、Python 依赖检查和平台参数，避免 CMake 误选 NMake。
- 所有新增 C 源码均写有中文注释。

### 4.2 USB 摄像头验证

硬件：LRCPG720p USB 摄像头，连接 ESP32-P4 高速 USB 口。

旧会话的完整串口日志已证明：

- 摄像头能被 UVC Host 正常枚举。
- 支持 MJPEG：640×480、640×360、800×600、1280×720、1280×960、720×960，多数为 30 FPS。
- 支持 YUY2：640×480 和 640×360 为 30 FPS，800×600 为 20 FPS，1280×720 和 1280×960 为 10 FPS。
- 640×480 MJPEG 30 FPS 已连续稳定收帧。
- 日志中帧数和字节数持续增加，约 32 KB/帧，空帧数为 0，没有流错误。

先前出现 `Could not find frame format ... @0.0FPS` 的原因是程序把帧率传成了 0。重写后的程序先读取摄像头实际描述符并使用有效帧率，不再将 0 FPS 当作“设备默认值”。

### 4.3 ST7789 屏幕测试

屏幕：GMT020-02-8P，驱动芯片 ST7789，当前按 240×320、RGB565、4 线 SPI 配置。

| 屏幕引脚 | ESP32-P4 |
| --- | --- |
| GND | GND |
| VCC | 3V3 |
| SCL / SCLK | GPIO20 |
| SDA / MOSI | GPIO32 |
| RST / RES | GPIO3 |
| DC | GPIO2 |
| CS | GPIO1 |
| BL / BLK | 3V3 |

测试程序已经实现：

- ST7789 初始化、复位、显示开启及 LVGL SPI DMA 显示端口。
- LVGL 8.4 绘制 EXP-01 中性待机、EXP-02 微笑和 EXP-03 开心三个表情。
- 中性待机具有随机眨眼和轻微呼吸；微笑使用弧线眼睛；开心具有红色脸颊、张嘴和上下弹动。
- 三个表情按 5 秒、2.5 秒、3 秒循环显示，串口同步输出当前表情 ID 和名称。

屏幕模式已完整编译通过：

```text
build_screen_verified/lummiss_hardware_test.bin
应用大小：0x752f0，8 MB 应用分区剩余约 94%
Bootloader：0x5310，Bootloader 分区剩余约 13%
镜像芯片范围：ESP32-P4 v1.0–v1.99，与实测 v1.3 芯片兼容
```

## 5. 构建和烧录

### 5.1 当前默认：屏幕测试

在 ESP-IDF 5.5.5 终端中运行：

```powershell
Set-Location E:\Lummiss_Plant_Robot\src\demo
idf.py -B build_screen_verified -DLUMMISS_TEST=screen build
idf.py -B build_screen_verified -p COM17 flash monitor
```

也可直接用 VS Code 的 ESP-IDF“构建、烧录、监视”任务。`.vscode/settings.json` 已将当前模式设为 `screen`。

### 5.2 切换为摄像头测试

```powershell
Set-Location E:\Lummiss_Plant_Robot\src\demo
idf.py -B build_camera_verified -DLUMMISS_TEST=camera build
idf.py -B build_camera_verified -p COM17 flash monitor
```

两个模式使用不同构建目录，避免 CMake 缓存互相覆盖。摄像头模式已按 ESP32-P4 v1.x 重新构建通过，应用大小为 `0x43eb0`。

## 6. 屏幕实机验收现象

烧录屏幕版本后，正常现象如下：

1. 背光上电后常亮，屏幕背景为黑色，串口出现 `FACE_DEMO` 初始化信息。
2. EXP-01 中性待机显示约 5 秒：白色圆眼、黑色瞳孔和红色短嘴，期间随机眨眼并轻微上下呼吸。
3. EXP-02 微笑显示约 2.5 秒：两只白色弧线眼睛和红色弧线笑嘴。
4. EXP-03 开心显示约 3 秒：挤眼笑、红色脸颊、张嘴和粉色舌头，整体快速轻微上下弹动。
5. 随后回到 EXP-01 循环，串口每次切换都会输出表情 ID 和名称，全程不重启、不花屏。

需要记录的异常及对应参数：

| 异常 | 首要检查项 |
| --- | --- |
| 背光亮但全黑/全白 | CS、DC、RST、SCLK、MOSI 接线以及屏幕供电 |
| 红色和蓝色互换 | `rgb_ele_order` 在 RGB/BGR 之间切换 |
| 黑白或所有颜色反相 | `esp_lcd_panel_invert_color()` 的参数 |
| 图案整体偏移或边缘缺失 | `esp_lcd_panel_set_gap()` 的 X/Y 偏移值及实际分辨率 |
| 横竖方向错误 | `swap_xy` 和 `mirror` 参数 |
| 随机噪点或刷新不稳 | 降低 SPI 时钟、缩短排线、检查共地和 3.3 V 稳定性 |

当前代码采用 `RGB`、颜色反相开启、偏移 `(0, 0)`、竖屏 240×320、SPI 20 MHz。这些是可行性测试初值，最终值必须以实机现象为准。

## 7. 当前已知问题和需求缺口

### 7.1 当前接线无法实现产品要求的背光调节

BL 直接接 3V3 时只能常亮，程序无法实现夜间低亮度、休眠熄屏和低电量降亮度。正式硬件需要把 BL 接到可承载背光电流的驱动电路，由支持 PWM 的 GPIO 控制；不能在未确认电流前把背光直接接普通 GPIO。

### 7.2 屏幕型号参数仍需实机确认

目前缺少 GMT020-02-8P 模组的完整规格书，240×320 分辨率、RGB/BGR、颜色反相和显存偏移仍是合理初值。屏幕基础测试通过后，应把最终参数记录到硬件接口文档。

### 7.3 产品输入方式需要统一

屏幕交互文档规定屏幕本身不承担点击，输入来自语音和机身左右电容触摸区。因此不要为当前 SPI 屏幕增加触摸屏逻辑。右侧单击与双击共用 350 ms 判定窗口，右侧单击必然存在最多约 350 ms 的等待，应明确这是否符合交互验收。

### 7.4 低功耗与“睡眠表情持续显示”存在硬件取舍

ESP32-P4 进入深睡眠后不能持续刷新动画。文档中的低亮度睡眠表情更接近低帧率待机状态，不能直接等同于深睡眠。需要分别定义“屏幕待机”和“系统深睡眠”的触发条件、功耗目标及唤醒源。

### 7.5 网络架构尚未定稿

ESP32-P4 本身不集成 Wi-Fi；厂商资料中包含 ESP32-C6 网络适配器固件。正式方案需要确定 P4 与 C6 的物理接口、通信协议、固件版本管理和异常恢复。需求同时要求 HTTPS、WebSocket 和 MQTT/TCP，但没有明确每类消息使用哪个通道，容易造成重复实现和状态不一致。

### 7.6 外设规格和引脚仍不完整

植物传感器、左右电容触摸、电机/旋转底座、麦克风、功放/扬声器、电池计量及充电管理芯片尚未给出最终型号、供电、电平和引脚表。未确定这些信息前，只能搭建软件接口，不能完成可验收的驱动。

### 7.7 AI 与离线能力边界需要明确

需求中同时出现云端 AI、断网基础语音对话、本地人格/记忆缓存等描述，但没有定义哪些模型和数据在 ESP32-P4/C6 上运行。需要明确 ASR、唤醒词、TTS、对话模型和情绪识别分别在设备端、手机端还是云端完成，并给出接口超时和降级策略。

### 7.8 表情资源尚未形成固件输入

文档列出 50 种动态表情，但仍缺少最终动画文件、像素尺寸、帧率、色深、透明方式、资源命名、压缩格式和 Flash/SD 卡存储方案。状态机开发前应先冻结一套最小资源包。

### 7.9 数据永久销毁需要单独设计

Flash、NVS 和 SD 卡可能存在磨损均衡及残留副本，普通文件删除不能保证不可恢复。若产品保留“一键永久销毁”验收，应优先采用数据加密和密钥销毁，并明确设备密钥、备份、云端副本和恢复策略。

## 8. 下一步任务顺序

### P0：完成屏幕实机可行性验证

- 烧录当前 LVGL `screen` 模式并记录三个表情的照片、视频和完整串口日志。
- 确认 EXP-01 的圆眼、黑色瞳孔、红色短嘴、随机眨眼和轻呼吸均正常。
- 确认 EXP-02 的弧线眼睛和笑嘴方向正确，EXP-03 的挤眼、脸颊、张嘴、舌头和弹跳完整可见。
- 确认背景为黑色、红白色序正确、有效区域完整、竖屏方向正确，切换时无明显撕裂或闪白。
- 若显示异常，优先调整 RGB/BGR、颜色反相、显存偏移、方向或 SPI 时钟，再调整表情坐标。
- 连续循环至少 30 分钟，确认无花屏、刷新停滞、内存异常和重启。

### P1：补完摄像头工程验证

- 用当前重写版本再次验证 640×480 MJPEG 30 FPS。
- 测试 1280×720 MJPEG，并记录实际帧率、平均帧大小和内存占用。
- 测试运行中拔出再插入摄像头，确认程序能释放并重新打开设备。
- 增加单帧保存或 JPEG 解码显示，证明收到的数据可用，而不仅是字节数增加。

### P2：建立产品软件骨架

- 把 LCD、UVC、音频、传感器、触摸、电机、网络、电源拆成独立组件。
- 建立统一事件队列和状态机，实现文档中的状态优先级与超时回落。
- 在当前 LVGL 显示端口基础上，把表情绘制、动画和表情切换接口从 `screen_test.c` 拆为独立组件。
- 实机参数稳定后建立最小表情资源包及统一的表情 ID、播放时长和优先级接口。
- 建立硬件引脚表、公共错误码、模块 API 文档和测试记录。

### P3：按硬件到位顺序集成

建议顺序为：音频采集/播放 → 左右电容触摸 → 植物传感器 → 电机/旋转底座 → P4/C6 网络 → 电池与功耗 → 云端协议 → 完整表情与 AI 状态联动。

## 9. 给下一个会话的操作提示

1. 先阅读本文件，再阅读 `src/demo/README.md`。
2. 当前最直接的输入是用户烧录屏幕版本后的显示现象和串口日志。
3. 修改代码继续写中文注释。
4. 构建必须使用 ESP-IDF 5.5.5；电脑中还存在其他 IDF 版本，终端环境选错会导致依赖和配置不一致。
5. 保持项目根路径为 `E:\Lummiss_Plant_Robot`，避免重新引入中文路径构建问题。
6. 不要删除厂商示例或已经通过的两个构建目录；需要清理时只针对明确的单个测试构建目录。
